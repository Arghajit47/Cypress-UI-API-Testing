const properties = {
  baseUrl: Cypress.config().baseUrl,
  FullName: Cypress.config().FullName,
  Email: Cypress.config().Email,
  Password: Cypress.config().Password,
  UserName: Cypress.config().UserName,
};

export default properties;
